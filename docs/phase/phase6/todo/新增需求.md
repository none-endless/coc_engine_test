当前提示词里实现是通过复用之前的DSL语句来实现的一致性维护
我希望改成这样:
  系统自动收集叙事信息,超过指定条数的description,以及shortLog,按照列表的形式提供给llm,llm收集描述,shortLog,叙事信息,最后返回一个列表,列表第一个必须是压缩后的叙事信息,从第二个开始才是压缩后的public与shortLog,格式如下[
            {kind:narration/description/key_facts,
             value:narration是几句话/description是最多两句话/keyfacts是一句话
            }...
            ]
不使用原来的add这些语句了,系统自动清空add列表留下空列表,ShortLog也被清空,叙事信息也只留下被压缩后的那两三句话,这样llm需要输出的信息只有被压缩后的信息,不需要输出冗杂的json格式信息,输出被进一步压缩,同时系统鲁棒性提高,你需要更改信息收集系统,提示词,json,数据模型等一条链路,我希望你能完整实现