1.我希望dm_agent的对话信息只记录reply的而不是什么都记录,这和这个字段的语义没有对齐 2.Log就不应该被读入到llm的上下文内,对于dm的log读取的是多余的dialogues(超过k条,最后一条自动挤入),且不应该进入llm,narrativeInfo读取的是merger输出的结果,但不应该被喂入llm 3. 一致性agent的实现和我想法相符,不用更改,我只想要保障能够读入完整rencentInfo并压缩,读入超过阈值的add并压缩给public,读取超过阈值的short_log并压缩到key_fact当中 4.dm_agent和npc_performer应该那到完整的所有id(还要包括姓名)和属性 5.清除dm_agent中没有用到的字串,比如key_fact,current_event 6.为performer_agent和dm_agent提供完整的可用属性和人物id