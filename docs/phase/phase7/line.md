### Phase 7：全链路测试、调试工具与部署准备 (Testing & Ops)

**目标**：完善调试可视化、全链路压测、错误注入测试，准备生产环境部署物料。

#### 需要做什么
1.  **调试工具 (play&&Debug UI)**：
    -   使用streamlit开发简单的 Web 面板，对游玩ui,包含对话框,世界选择,聊天框输入,api-key-url配置,对玩家可见信息(narrative_agent输出的信息)输出流式展示的web界面(使用streamlit实现),开启debug模式后对于debugui展示当前`Trace ID`、`世界快照`、`叙事池` 和 `链路日志 (E1-E7)`,以及每个agent的完整输入输出信息(可以点按钮切换不同的agent,通过格式化形式点左右箭头展示不同回合的输入输出)。
2.  **文档与部署**：
    -   编写开发者手册:教授如何使用该引擎制作游戏
    -   整理当前实现手册
3.  **清理代码**
    -   整理代码注释
    -   清理冗余代码
    -   合并兼容性或低质量或妥协性实现转而使用等效的优雅可增量开发实现

#### 验收需求
-   开发者可以通过 `trace_id` 追踪一笔玩家输入以及npc输入从 `InputSystem` 到 `MergerAgent` 的完整生命周期。

#### 生产待检查物料
-   [ ] 包含对话框,世界选择,聊天框输入,api-key-url配置,对玩家可见信息输出流式展示的web界面(使用streamlit实现)
-   [ ] 系统架构说明
-   [ ] 全流程 `trace_id` 链路追踪（展示从 E1 到 E7 的数据流）。
-   [ ] 完整的开发指南手册


