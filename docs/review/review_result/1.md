
**审查结论**
- 当前总体进度：进行中（核心到 Phase3 基本落地，Phase4 以后未闭环）


**问题清单（按严重度）**



2. 严重：扩展字段写入未按 schema registry 的 mutable/type 约束执行  
证据：
- state_patch.py 到 state_patch.py 只检查 extensions 字段是否存在，不检查 registry 中 mutable 和 value_type  
- 模型层已定义 registry 能力：base.py  
为什么是问题：规范明确 extensions 必须受 registry 权限约束；目前实现会出现“字段存在即可写”，越权风险高。

3. 高：鉴定链路被压缩为单属性单目标，against 语义未闭环  
证据：
- engine.py 到 engine.py 的 _run_check 只取 attrs[0]  
- engine.py 和 engine.py 虽识别 num/against，但后续未基于 against_char_id 执行对抗流程  
为什么是问题：规范要求支持数值鉴定/对抗鉴定及参与对象；当前实现只覆盖简化单次判定，会限制交互自由度。

4. 高：Merger 与一致性维护链路未落地（Phase4/6 关键缺口）  
证据：
- 仅有模型定义，无实现文件：agent_input.py、agent_output.py  
- 找不到 merger llm 实现文件：src/agent/llm 下无 merger  
- 找不到 consistency agent 实现文件：src/agent/llm 下无 consistency  
为什么是问题：规范要求世界真值与叙事真值双池维护、narrative 草稿提交后合并、定期一致性修复；当前属于“数据结构先行，业务未闭环”。

5. 中：StatePatch 的 ADD 对嵌套列表无防护，可能污染列表结构  
证据：
- state_patch.py 直接 current.append(entry)，若 entry 本身是 list 会形成嵌套  
为什么是问题：会造成后续逻辑和去重异常（这类问题在状态补丁系统里是高频隐患）。

6. 中：回滚后的 retry_count 语义可能偏差（统计不一致）  
证据：
- engine.py 返回 retry_count=max_retry  
为什么是问题：循环是 max_retry + 1 次尝试，外部日志会误读“实际尝试次数 vs 可重试次数”，不利于运维排障。

7. 低：规则系统存在提醒注释与职责漂移信号  
证据：
- rule_system.py 和 rule_system.py 有临时性注释  需要根据注释需求进行更改

**上下游闭环缺口**
- 数据模型已定义，但调用方未接入：NpcPerformer、Merger、Consistency（模型有，执行链无）。
- 配置有预算项，但业务未消费：max_actions_per_turn、cooldown_turns 只在配置层出现，loader.py。
- Prompt 与实现不一致：已有 merger prompt 资源，但缺少对应 llm agent 与 engine 调用。
- 文档/证据链断层：Phase4-7 没有完成证据文件（目前你让我不跑运行，我仅按静态文件和实现判定）。

**阶段进度判定（按技能规则）**
- Phase0：已完成。证据 phase0_foundation_done.md
- Phase1：已完成。证据 phase1_input_rule_done.md
- Phase2：已完成。证据 phase2_dm_evolution_done.md
- Phase3：部分完成。状态补丁与并发框架在，但仍有扩展权限校验与计数语义问题。证据 phase3_concurrent_statechange_done.md
- Phase4：进行中（有 Narrative，但无 Merger 落地）
- Phase5：未完成（NpcPerformer 空实现）
- Phase6：未完成（Consistency 未实现）
- Phase7：未开始（调试与部署闭环未见实现证据）


如果你要，我下一步可以直接给你一版“最小重构清单（按 2 周迭代）”，只列必须改的文件和改动顺序。